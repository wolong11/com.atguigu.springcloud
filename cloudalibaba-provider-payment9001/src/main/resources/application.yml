server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # config nacos address
management:
  endpoints:
    web:
      exposure:
        include: "*"
