server:
  port: 3366

spring:
  application:
    name: cloud-config-client
  cloud:
    config:
      label: main
      name: config
      profile: dev
      uri: http://config-3344.com:3344
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka

# exposure monitor endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"  #refresh
